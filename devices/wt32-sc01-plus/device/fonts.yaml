# =============================================================================
# FONTS (DEVICE OVERRIDE) - Smaller fonts for the 320x480 portrait display
# =============================================================================
# Supplements the common fonts with smaller variants for the main UI.
# The common fonts (icon_font, label_font, etc.) still load for setup screens;
# these _sm variants are referenced by the device theme and LVGL config.
# Sized to match the 4848S040 (similar physical cell width); shared font ids
# keep the device theme and grid wiring code consistent across S3 panels.
# =============================================================================

font:
  # Button icons (46px, down from common 52px)
  - file: 'https://github.com/Templarian/MaterialDesign-Webfont/raw/v7.4.47/fonts/materialdesignicons-webfont.ttf'
    id: icon_font_sm
    size: 46
    bpp: 4
    glyphs: !include ../../../common/assets/icon_glyphs.yaml

  # Button labels (22px)
  - file: "gfonts://Roboto"
    id: label_font_sm
    size: 22
    bpp: 4
    glyphs: !include ../../../common/assets/text_glyphs.yaml

  # Climate detail page: oversized target temperature
  - file: "gfonts://Roboto@Light"
    id: climate_target_font_sm
    size: 106
    bpp: 4
    glyphs:
      - "0123456789.-"

  # Forecast high / low values (smaller than one-number sensor values)
  - file: "gfonts://Roboto@Light"
    id: forecast_font_sm
    size: 34
    bpp: 4
    glyphs:
      - "0123456789.- /"

  # Top bar clock and temperature (20px)
  - file: "gfonts://Roboto"
    id: clockbar_font_sm
    size: 20
    bpp: 4
